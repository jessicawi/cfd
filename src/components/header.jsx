import React from "react";
import Layout from "./layout/layout";

const Header = () => {
    return (
        <React.Fragment>
            <Layout/>
        </React.Fragment>
    );
};

export default Header;
